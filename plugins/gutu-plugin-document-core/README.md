# gutu-plugin-document-core

This folder is the intended standalone git repository for the `document-core` plugin.

Source lives under:

- `framework/builtin-plugins/document-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
