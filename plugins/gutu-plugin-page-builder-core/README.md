# gutu-plugin-page-builder-core

This folder is the intended standalone git repository for the `page-builder-core` plugin.

Source lives under:

- `framework/builtin-plugins/page-builder-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
