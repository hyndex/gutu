# gutu-plugin-notifications-core

This folder is the intended standalone git repository for the `notifications-core` plugin.

Source lives under:

- `framework/builtin-plugins/notifications-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
