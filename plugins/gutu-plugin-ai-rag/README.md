# gutu-plugin-ai-rag

This folder is the intended standalone git repository for the `ai-rag` plugin.

Source lives under:

- `framework/builtin-plugins/ai-rag`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
