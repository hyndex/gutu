# gutu-plugin-dashboard-core

This folder is the intended standalone git repository for the `dashboard-core` plugin.

Source lives under:

- `framework/builtin-plugins/dashboard-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
