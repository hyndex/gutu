# gutu-plugin-org-tenant-core

This folder is the intended standalone git repository for the `org-tenant-core` plugin.

Source lives under:

- `framework/builtin-plugins/org-tenant-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
