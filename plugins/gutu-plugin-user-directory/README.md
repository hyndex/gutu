# gutu-plugin-user-directory

This folder is the intended standalone git repository for the `user-directory` plugin.

Source lives under:

- `framework/builtin-plugins/user-directory`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
