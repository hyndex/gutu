# gutu-plugin-files-core

This folder is the intended standalone git repository for the `files-core` plugin.

Source lives under:

- `framework/builtin-plugins/files-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
