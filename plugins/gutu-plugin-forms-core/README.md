# gutu-plugin-forms-core

This folder is the intended standalone git repository for the `forms-core` plugin.

Source lives under:

- `framework/builtin-plugins/forms-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
