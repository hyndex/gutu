# gutu-plugin-jobs-core

This folder is the intended standalone git repository for the `jobs-core` plugin.

Source lives under:

- `framework/builtin-plugins/jobs-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
