# gutu-plugin-admin-shell-workbench

This folder is the intended standalone git repository for the `admin-shell-workbench` plugin.

Source lives under:

- `framework/builtin-plugins/admin-shell-workbench`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
