# gutu-plugin-role-policy-core

This folder is the intended standalone git repository for the `role-policy-core` plugin.

Source lives under:

- `framework/builtin-plugins/role-policy-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
