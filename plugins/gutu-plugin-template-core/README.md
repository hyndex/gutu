# gutu-plugin-template-core

This folder is the intended standalone git repository for the `template-core` plugin.

Source lives under:

- `framework/builtin-plugins/template-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
