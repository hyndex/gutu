# gutu-plugin-ai-evals

This folder is the intended standalone git repository for the `ai-evals` plugin.

Source lives under:

- `framework/builtin-plugins/ai-evals`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
