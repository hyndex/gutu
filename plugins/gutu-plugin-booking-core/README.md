# gutu-plugin-booking-core

This folder is the intended standalone git repository for the `booking-core` plugin.

Source lives under:

- `framework/builtin-plugins/booking-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
