# gutu-plugin-ai-core

This folder is the intended standalone git repository for the `ai-core` plugin.

Source lives under:

- `framework/builtin-plugins/ai-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
