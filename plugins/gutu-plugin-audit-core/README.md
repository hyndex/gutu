# gutu-plugin-audit-core

This folder is the intended standalone git repository for the `audit-core` plugin.

Source lives under:

- `framework/builtin-plugins/audit-core`

This repo was extracted from the previous monolithic workspace so the plugin can be developed independently from `gutu-core`.
