# Changelog

## 0.0.1 - 2026-04-20

- Archived the previous repository contents under `old_contents/`.
- Rebuilt the repository as a clean, plugin-free `gutu-core` baseline.
- Added fresh core packages for `kernel`, `ecosystem`, and `cli`.
