# gutu-lib-data-table

This folder is the intended standalone git repository for the `data-table` library.

Source lives under:

- `framework/libraries/data-table`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
