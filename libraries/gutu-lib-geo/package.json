{
  "name": "gutu-lib-geo",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun --cwd framework/libraries/geo run build",
    "typecheck": "bun --cwd framework/libraries/geo run typecheck",
    "lint": "bun --cwd framework/libraries/geo run lint",
    "test": "bun --cwd framework/libraries/geo run test",
    "test:unit": "bun --cwd framework/libraries/geo run test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
