# gutu-lib-geo

This folder is the intended standalone git repository for the `geo` library.

Source lives under:

- `framework/libraries/geo`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
