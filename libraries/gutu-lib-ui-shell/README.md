# gutu-lib-ui-shell

This folder is the intended standalone git repository for the `ui-shell` library.

Source lives under:

- `framework/libraries/ui-shell`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
