# gutu-lib-admin-shell-workbench

This folder is the intended standalone git repository for the `admin-shell-workbench` library.

Source lives under:

- `framework/libraries/admin-shell-workbench`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
