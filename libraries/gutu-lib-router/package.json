{
  "name": "gutu-lib-router",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun --cwd framework/libraries/router run build",
    "typecheck": "bun --cwd framework/libraries/router run typecheck",
    "lint": "bun --cwd framework/libraries/router run lint",
    "test": "bun --cwd framework/libraries/router run test",
    "test:unit": "bun --cwd framework/libraries/router run test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
