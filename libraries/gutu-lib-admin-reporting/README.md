# gutu-lib-admin-reporting

This folder is the intended standalone git repository for the `admin-reporting` library.

Source lives under:

- `framework/libraries/admin-reporting`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
