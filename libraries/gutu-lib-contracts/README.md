# gutu-lib-contracts

This folder is the intended standalone git repository for the `contracts` library.

Source lives under:

- `framework/libraries/contracts`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
