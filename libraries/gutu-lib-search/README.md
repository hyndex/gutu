# gutu-lib-search

This folder is the intended standalone git repository for the `search` library.

Source lives under:

- `framework/libraries/search`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
