# gutu-lib-ui-editor

This folder is the intended standalone git repository for the `ui-editor` library.

Source lives under:

- `framework/libraries/ui-editor`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
