# gutu-lib-ai-memory

This folder is the intended standalone git repository for the `ai-memory` library.

Source lives under:

- `framework/libraries/ai-memory`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
