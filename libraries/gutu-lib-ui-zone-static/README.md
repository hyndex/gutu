# gutu-lib-ui-zone-static

This folder is the intended standalone git repository for the `ui-zone-static` library.

Source lives under:

- `framework/libraries/ui-zone-static`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
