{
  "name": "gutu-lib-layout",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun --cwd framework/libraries/layout run build",
    "typecheck": "bun --cwd framework/libraries/layout run typecheck",
    "lint": "bun --cwd framework/libraries/layout run lint",
    "test": "bun --cwd framework/libraries/layout run test",
    "test:unit": "bun --cwd framework/libraries/layout run test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
