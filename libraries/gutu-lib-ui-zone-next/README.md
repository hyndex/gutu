# gutu-lib-ui-zone-next

This folder is the intended standalone git repository for the `ui-zone-next` library.

Source lives under:

- `framework/libraries/ui-zone-next`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
