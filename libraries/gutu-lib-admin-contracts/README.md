# gutu-lib-admin-contracts

This folder is the intended standalone git repository for the `admin-contracts` library.

Source lives under:

- `framework/libraries/admin-contracts`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
