# gutu-lib-admin-widgets

This folder is the intended standalone git repository for the `admin-widgets` library.

Source lives under:

- `framework/libraries/admin-widgets`

This repo was extracted from the previous monolithic workspace so the library can be developed independently from `gutu-core`.
