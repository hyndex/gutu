# gutu-app-platform-dev-console

This folder is the intended standalone git repository for the `platform-dev-console` app.

Source lives under:

- `apps/platform-dev-console`

This repo was extracted from the previous monolithic workspace so the app can be developed independently from `gutu-core`.
