# gutu-app-playground

This folder is the intended standalone git repository for the `playground` app.

Source lives under:

- `apps/playground`

This repo was extracted from the previous monolithic workspace so the app can be developed independently from `gutu-core`.
