# TEST_MATRIX

| Component / Package Group | Required Test Types | Present | Known Gaps | Status |
| --- | --- | --- | --- | --- |
| Workspace foundation | smoke, lint, typecheck, build, release, e2e gate | clean-shell root `ci:check`, repo-wide lint/typecheck/build, workspace scaffolder, README/runbook, release pipeline doc, GitHub workflows, release bundle packaging, coverage reporting, SBOM/provenance/audit scripts, browser E2E wired into `ci:check` | none for the framework/package acceptance scope | complete |
| `packages/kernel` | unit, contract | manifest DSL tests, registry tests, deterministic validation errors exercised through solver/package tests | signed-package/provenance compatibility regressions can grow later, but baseline package contract coverage is in place | complete |
| `packages/schema` | unit, contract | resource/action/json-schema tests, action execution validation, JSON schema generation coverage | richer schema snapshot permutations can grow later without blocking the framework baseline | complete |
| `packages/permissions` | unit, security, contract | policy, capability diffing, install/update review planning, dormant-grant reset metadata, optimistic grant-store conflict tests, unknown-plugin restriction tests, dangerous-capability lifecycle coverage | egress/secret enforcement is primarily exercised through `runtime-bun` and config integration rather than a separate permissions sandbox | complete |
| `packages/plugin-solver` | unit, contract, security | synthetic graph tests, real workspace bundle smoke tests, rollback checkpoint planning, dangerous-capability acknowledgement warnings, ownership conflict regressions, duplicate activation coverage, restricted-preview activation for unknown plugins, partial bundle activation coverage | deeper plugin/business install flows are deferred with plugin work | complete |
| `packages/db-drizzle` | unit, integration, migration | sqlite client/unit tests, role/RLS helper tests, generated Postgres bootstrap assets, live PostgreSQL integration test for roles/curated views/RLS, query convention tests | broader business-specific migration packs still live outside the shipped framework repo | complete |
| `packages/migrate` | unit, integration, migration | ordering, dry-run, rollback, metadata tests, representative migration-pack dry-run tests, live Postgres apply/rollback orchestration tests for canonical booking DDL | source-specific migration-pack implementations are still future/plugin-store work | complete |
| `packages/auth` / `packages/auth-admin` | unit, integration, security | tenant isolation, impersonation, session refresh/invalidation, header bridging, job propagation, admin-guard and audit-emission tests | external provider sandboxing is adapter-level follow-on work | complete |
| `packages/api-rest` | unit, contract, integration | route generation, action execution, OpenAPI, webhook signing, live Bun HTTP bridge tests | richer replay/failure simulations can grow later without blocking the baseline | complete |
| `packages/api-graphql` | unit, contract, integration | generated query/mutation tests, optional mount tests, live Bun HTTP bridge tests | typed field synthesis beyond JSON boundaries is a future enhancement, not a baseline gap | complete |
| `packages/ui-shell` and UI wrappers | unit, UI smoke, integration, browser e2e | shell registry tests, shared provider/navigation/telemetry tests, route/deep-link tests, UI wrapper unit tests, browser-rendered admin/portal/site harness E2E | none for the framework/package acceptance scope | complete |
| Canonical admin wrapper aliases (`packages/ui`, `router`, `query`, `data-table`, `form`, `chart`, `editor`, `layout`, `contracts`, `telemetry-ui`, `command-palette`) | unit, contract, integration, browser e2e, policy | wrapper unit suites, alias behavior tests, admin-plugin raw-import contract scan, workbench/browser flows using the canonical wrappers, clean-shell root `ci:check` after migration | package entrypoints still emit warning-only `react-refresh/only-export-components` messages because they intentionally export helpers plus components | complete |
| `packages/admin-contracts` | unit, contract, security | contribution registry tests, deterministic conflict tests, permission-helper tests, legacy `defineUiSurface` adapter tests | broader snapshot expansion can grow later without blocking the baseline | complete |
| `packages/admin-listview` | unit, contract | list-definition tests, list-state tests, saved-view serialization tests, resource-derived list-view tests | server-backed list execution adapters can deepen later | complete |
| `packages/admin-formview` | unit, contract | form/detail definition tests, dirty-state tests, async-validation tests, resource-derived form/detail tests | richer field-policy snapshots can deepen later | complete |
| `packages/admin-widgets` | unit, contract, UI smoke | widget primitive render tests, dashboard/saved-question tests, ECharts option generation tests | client-side chart hydration can deepen later | complete |
| `packages/admin-reporting` | unit, contract | semantic query tests, report execution request tests, filter validation tests | richer backend report execution can deepen later | complete |
| `packages/admin-builders` | unit, contract, UI smoke | builder definition tests, publish-conflict tests, builder host render tests | builder-specific zone workflows can deepen later | complete |
| `packages/admin-shell-workbench` | unit, integration, security, UI smoke | registry composition tests, route resolution tests, permission filtering tests, search-provider isolation tests, desk render tests, workbench customization/typecheck verification, query-preserving command/search/theme state handling, shell-owned chrome/theming exercised through the browser harness | no known package-level gap in this wave | complete |
| `framework/libraries/ai` | unit, contract | provider-neutral tool contract tests, action metadata mapping, approval/redaction helpers, replay metadata coverage | provider adapters remain connector-owned rather than leaking into business code | complete |
| `framework/libraries/ai-runtime` | unit, contract, replay | agent definition validation, run lifecycle tests, approval checkpoint tests, budget consumption, denial/replay mismatch coverage | persistence-backed orchestration can deepen later without blocking the framework baseline | complete |
| `framework/libraries/ai-memory` | unit, contract, security | collection/document/chunk tests, retrieval plan generation, citation serialization, tenant/policy access checks | external vector/search backends are still adapter-stage follow-on work | complete |
| `framework/libraries/ai-guardrails` | unit, security | prompt sanitization, blocked substring checks, PII redaction patterns, tool-risk assessment, output moderation tests | provider-backed moderation adapters can deepen later | complete |
| `framework/libraries/ai-evals` | unit, contract | dataset/run/baseline/regression-gate tests, comparison helpers, judge contract coverage, root eval-threshold gate wiring | provider-backed eval breadth can deepen later, but the release gate is wired | complete |
| `framework/libraries/ai-mcp` | unit, contract, transport | tool/resource/prompt descriptor derivation tests, server-definition generation tests, JSON-RPC runtime dispatch tests, stdio transport exercised through CLI tests | external connector fleet and remote transport governance are still follow-on work | partial |
| `framework/libraries/communication` | unit, contract | channel compiler tests for `email`/`sms`/`push`/`in-app`, email-template delegation tests, deterministic local-provider outcome tests, retry-decision tests, callback normalization tests, idempotency-key derivation tests | live provider SDK connectors remain connector-owned and intentionally out of repo scope | complete |
| `framework/core/ecosystem` | unit, lockfile, resolver, catalog | repo-name derivation tests, lockfile/override schema tests, transitive vendoring-plan tests, relative source-path regression coverage, generated catalog/channel freshness check in root CI | live GitHub repo/release validation remains future work until the external `gutula` repo fleet exists | partial |
| `framework/core/agent-understanding` and docs CLI flow | unit, contract, repo validation | understanding-doc pack tests, schema/action/workflow semantic metadata coverage through contract tests, CLI scaffold/index/validate tests, generated `docs/agent-understanding.index.json`, root `docs:validate` wired into `ci:check` | many older packages still emit warning-level gaps for thin inline semantic descriptions even though required doc-pack coverage is now present repo-wide | partial |
| `packages/jobs` / `packages/events` | unit, integration, concurrency | unit package tests, idempotency/outbox coverage, in-memory queue tests, reentrant dedupe coverage, BullMQ integration against live Redis-backed worker | workload-specific plugin orchestration is deferred with plugin work | complete |
| `packages/observability` / `packages/logger` | unit, integration | request/job context logging, redaction, telemetry bootstrap, span wrapper tests | provider/exporter sandbox permutations can grow later without blocking the baseline | complete |
| Built-in AI packs (`ai-core`, `ai-rag`, `ai-evals`) | manifest, unit, contract, admin integration | manifest tests, persisted operator service tests, admin contribution contract tests, AI workspace/search/report/widget coverage in the browser harness | control-plane state is file-backed locally, but deeper DB/workflow-backed multi-node durability is still follow-on work | partial |
| `framework/core/cli` AI + ecosystem surface | unit, contract, transport smoke | `platform agent/prompt/memory/eval/mcp/make ai-pack` tests, `gutu init`, `gutu add`, `gutu update`, `gutu vendor sync`, `gutu override add/remove`, `gutu ecosystem doctor`, catalog export, standalone repo scaffold coverage, MCP inspection coverage, stdio MCP handshake/list smoke, cross-platform init behavior coverage | full workspace/runtime orchestration, remote artifact fetch, and external connector lifecycle remain follow-on work | partial |
| Foundation plugins | manifest, unit, integration, permission | manifest tests, UI surface contract tests, foundation dependency graph normalized, richer unit/action coverage for `auth-core`, `org-tenant-core`, `role-policy-core`, `audit-core`, `user-directory`, `workflow-core`, `jobs-core`, `files-core`, `notifications-core`, `portal-core`, and `booking-core` | remaining foundations still need broader persistence/policy behavior and integration suites beyond the now-shipped booking substrate | partial |
| Communication baseline (`notifications-core` + communications workbench) | manifest, unit, integration, migration, browser e2e | endpoint/preference/message/attempt action tests, lifecycle integration tests for queue/dispatch/retry/cancel/callback reconciliation, UI contract tests for `/admin/communications/*`, Postgres DDL/index tests, dev-console browser E2E for canonical and compatibility routes | live third-party connector packages and inbound delivery flows remain follow-on work | partial |
| External plugin-store packages | manifest, unit, integration, permission, migration where applicable | support remains in the manifest DSL, solver, and CLI planning docs; the shipped repo no longer includes the optional plugin catalog | these packages are intentionally out of scope for the framework-only Git baseline and move to the future plugin store | deferred |
| Feature packs | manifest and resolver support only in the framework baseline | package-kind contracts, solver behavior, and documentation remain covered | no checked-in feature-pack catalog remains in this repository | deferred |
| Connectors | manifest and governance support only in the framework baseline | connector manifest validation, secret/host/webhook contracts, and provider-wrapper rules remain covered | live connector packages are intentionally moved out of the framework repo | deferred |
| Migration packs | manifest and workflow support only in the framework baseline | migration-pack validation, phase contracts, and docs remain covered | checked-in migration-pack implementations are intentionally removed from this repo | deferred |
| Bundles | contract and resolver support | bundle manifest tests and plugin-solver bundle resolution smoke tests with inline manifests | installable bundle packages move to the plugin store | partial |
| Verticals / atomic subapps | topology support only | the framework can still host vertical packages through manifests and bundles | no vertical source packages are checked into the framework repo | deferred |
| Security model | permission denial, unknown plugin restrictions, egress denial, secret denial, DB denial | permission/package security tests, dormant-dangerous-grant handling, runtime host-allowlist tests, webhook verification, DB-denial helpers, clean supply-chain audit report with no allowlist | plugin/business secret workflows are deferred with plugin work | complete |
| Booking concurrency | concurrency, integration, E2E | canonical reservation writer/service tests, live Postgres reservation exclusion-constraint tests, live migration apply/rollback tests for booking DDL | browser-level booking E2E is still missing | partial |
| Auth / tenant / portal flow | integration, E2E | auth unit/security tests, portal plugin contract tests, portal route/widget registration tests, session/header/job propagation tests, browser E2E shell/session wiring via the dev-console harness | plugin-specific tenant business flows are deferred with plugin work | complete |
| Admin shell flow | integration, UI smoke, E2E | ui-shell contract tests, plugin page registrations, browser-level admin shell harness flow | plugin-specific admin business flows are deferred with plugin work | complete |
| Admin desk flow | unit, contract, integration, browser e2e | admin package unit suites, representative plugin admin-contribution contract suites, `@apps/platform-dev-console` direct Bun + Playwright E2E for workspaces/search/reports/builders/zones/forbidden routes/impersonation/restricted preview plus export center/report builder/job monitor and appearance-state preservation, fresh repo-wide lint green, exact clean-shell root `ci:check` green | no known framework/admin gap in this wave | complete |
| AI operator flow | unit, contract, integration, browser e2e, security | AI package family tests, AI pack contract tests, browser E2E for `/admin/ai/*` routes and AI reports, viewer-denial regression coverage, persisted prompt/approval/replay/retrieval/eval service coverage, release-gate CI wiring | provider-backed invocations and multi-node control-plane durability are still follow-on work | partial |
| Restricted-mode plugin flow | security, integration, E2E | unknown-plugin restriction tests in permissions/solver, restricted-preview activation path in solver, browser E2E via the dev-console harness proving stripped privileges and guarded access | none for the framework/package acceptance scope | complete |
| CI/CD pipeline | workflow smoke | GitHub Actions CI workflow, release-readiness workflow, artifact upload steps, release-pipeline documentation, coverage reporting, release bundle packaging, CycloneDX SBOM generation, provenance manifest generation, real local dev signing hook, CI env-signing contract, vulnerability audit automation, GitHub attestation steps | none for the framework/package acceptance scope | complete |
